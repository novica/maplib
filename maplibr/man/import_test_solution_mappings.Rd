% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/000-wrappers.R
\name{import_test_solution_mappings}
\alias{import_test_solution_mappings}
\title{Read an Arrow C stream from R back into a Struct-typed Series, unnest it
back into a DataFrame (\code{StructChunked::unnest}, the reverse of
\code{into_struct}), and return a small human-readable summary combining the
recovered DataFrame's shape with the \code{rdf_node_types} JSON side-channel
handed back in -- a sanity check that both halves survive the round trip
together, matching \code{import_test_series}'s role for the Series-only case.}
\usage{
import_test_solution_mappings(stream_ptr, rdf_node_types_json)
}
\arguments{
\item{stream_ptr}{An external pointer holding a filled ArrowArrayStream.}

\item{rdf_node_types_json}{The JSON string returned by
\code{export_test_solution_mappings}.}
}
\value{
A character vector: one line per column, \code{"<name>: <dtype> [<rdf types>]"}.
}
\description{
Read an Arrow C stream from R back into a Struct-typed Series, unnest it
back into a DataFrame (\code{StructChunked::unnest}, the reverse of
\code{into_struct}), and return a small human-readable summary combining the
recovered DataFrame's shape with the \code{rdf_node_types} JSON side-channel
handed back in -- a sanity check that both halves survive the round trip
together, matching \code{import_test_series}'s role for the Series-only case.
}
