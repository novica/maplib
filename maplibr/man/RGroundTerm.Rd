% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/000-wrappers.R
\name{RGroundTerm}
\alias{RGroundTerm}
\title{A ground SPARQL binding value: an IRI or a Literal (no blank nodes,
unlike \code{RConstantTerm} -- blank nodes can't be bound to a query variable,
so there's no variant for one to construct in the first place, unlike
py_maplib's equivalent, which has to reject a blank node at runtime since
it accepts a plain Python value that could be anything, see
\code{maybe_parse_bindings}, py_maplib/src/lib.rs:479-528). Built from an R
\code{IRI}/\code{Literal} (terms.R) by \code{.to_ground_term()}, and consumed by
\code{RModel::query}'s \code{bindings} argument.}
\usage{
RGroundTerm
}
\description{
A ground SPARQL binding value: an IRI or a Literal (no blank nodes,
unlike \code{RConstantTerm} -- blank nodes can't be bound to a query variable,
so there's no variant for one to construct in the first place, unlike
py_maplib's equivalent, which has to reject a blank node at runtime since
it accepts a plain Python value that could be anything, see
\code{maybe_parse_bindings}, py_maplib/src/lib.rs:479-528). Built from an R
\code{IRI}/\code{Literal} (terms.R) by \code{.to_ground_term()}, and consumed by
\code{RModel::query}'s \code{bindings} argument.
}
